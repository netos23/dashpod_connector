name: flutter_revision
description: Resolves the pinned Flutter SDK version for a Dart/Flutter package.
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.11.5

dependencies:
  path: ^1.9.0
  pub_semver: ^2.2.0
  yaml: ^3.1.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
